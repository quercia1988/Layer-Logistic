# Layer Logistic

App macOS nativa per gestire ordini, logistica e produzione di una ditta di stampa 3D.

## Avvio

```bash
swift run PrintFlow
```

In alternativa apri `Package.swift` con Xcode e premi Run. I dati vengono salvati in `~/Library/Application Support/PrintFlow/orders.json`.
